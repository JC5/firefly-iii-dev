# action.yml
name: 'Firefly III dev tools'
description: 'Run things bla bla bla'
inputs:
  action:  # id of input
    description: 'What to do'
    required: true
    default: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.action }}
